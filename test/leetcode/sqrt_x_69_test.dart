import 'package:arrays/leetcode/sqrt_x_69.dart';
import 'package:test/test.dart';

void main() {
  test('Sqrt(x)', () {
    expect(sqrt(0), 0);
    expect(sqrt(1), 1);
    expect(sqrt(9), 3);
    expect(sqrt(8), 2);
  });
}
